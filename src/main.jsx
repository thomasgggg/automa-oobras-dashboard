import React from "react";
import ReactDOM from "react-dom/client";
import CanteiroDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CanteiroDashboard />
  </React.StrictMode>
);
